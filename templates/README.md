# Templates

These are reference copies. The preferred way to bootstrap is:

```bash
rate-limit-handoff --init
```

which creates a ready-to-use `handoff.md` + `second_brain/` structure (including skills) in the current directory.
